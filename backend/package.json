{
  "name": "migrate-optima-backend",
  "version": "1.0.0",
  "description": "Standalone worker and metadata manager for MigrateOptima",
  "main": "dist/worker.js",
  "scripts": {
    "start": "node dist/worker.js",
    "dev": "ts-node-dev --respawn --transpile-only src/worker.ts",
    "build": "tsc",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "bullmq": "^5.4.1",
    "dockerode": "^4.0.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.25",
    "@types/node": "^20.11.20",
    "prisma": "^5.10.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
